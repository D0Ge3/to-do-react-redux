
const initialState = {
    items: [],
    totalCount: null,
    currentPage: 1
}

const tasksReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}


export default tasksReducer;